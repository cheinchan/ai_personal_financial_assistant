name: ai_personal_financial_assistant
description: AI Personal Financial Assistant mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  google_generative_ai: ^0.4.6  # Add this for Gemini API
  http: ^1.2.0  # Add this if not already present

  # Firebase - Core services
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # UI Components
  cupertino_icons: ^1.0.8

  # Charts
  fl_chart: ^0.69.0

  # Utilities
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Uncomment and add your assets here if needed
  # assets:
  #   - assets/images/
  #   - assets/icons/